# swipedmax-backend

## Production custom domain

The mobile app must call the API through `https://swiped-back.maximalsecurityservices.com`, not a hosting-provider URL or IP address. See [custom-domain.md](docs/custom-domain.md) for the DNS, TLS, backend environment, and DigitalOcean Spaces media-domain setup.
